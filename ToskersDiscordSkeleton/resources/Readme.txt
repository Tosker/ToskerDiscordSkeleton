Make sure all resources added have a copy-output to "Copy always".

For example: Click this readme.txt in the solution explorer. Visit it's properties panel, see "Copy to output directory", 
and set the drop-down to "Copy always".